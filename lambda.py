
# Lambda=-
           # A function is a small anonymouse function,
           
           # A lambda function can take any number of argument , but can only have one expression,
           
  # Syntex:
            # lambda a : a+10
            
             # the expression is excute the result is return,
      
  # Example:
  
  # Add 10 to argument a,
  
#x = lambda b : b+10
#print(x(5))                    

             # Multiple argument return result,

#x = lambda a,b,c : a*b+c
#print(x(4,4,4))             
  
                #__________****_________#
 
 
 
 
 
           